# MobiLoop MCP v0.1.0-alpha.12

Pre-release published on 2026-09-23.

## Highlights

- Rebuilt the public documentation site around a real device validation surface, test loop, security boundaries, and a directly runnable container command.
- Added a package-version resolver for MCP server initialization and regression coverage against release metadata.
- Kept the public site canonical URL, robots directives, sitemap, JSON-LD, social metadata, and Google Search Console verification metadata intact.

## Verification

- `npm test`: 57 passing tests.
- Lint, TypeScript typecheck, formatting, static-site metadata validation, package dry run, high-severity dependency audit, and Docker build passed.
- Pulled `ghcr.io/enessubass/mobiloop-mcp:0.1.0-alpha.11` into a clean local Docker cache and completed an MCP `initialize` smoke test before correcting the version source for this release.

## Distribution

- GitHub Container Registry: `ghcr.io/enessubass/mobiloop-mcp:0.1.0-alpha.12`
- MCP Registry package: `io.github.enessubass/mobiloop-mcp`
- Documentation site: `https://enessubass.github.io/mobiloop-mcp/`

The GitHub release remains a pre-release while MobiLoop is in the alpha channel. NPM publishing is not enabled.
