# MobiLoop MCP v0.1.0-alpha.5

Alpha security automation and native Android evidence release.

## Highlights

- Added a real Genymotion/Appium run artifact for the native Android Kotlin login fixture.
- Added Dependabot configuration for npm, Docker, and GitHub Actions update groups.
- Added CodeQL JavaScript/TypeScript scanning.
- Merged the npm development dependency update group after CI and CodeQL passed.
- Kept the Docker Node 26 runtime bump and GitHub Actions major-version bump open pending explicit runtime and self-hosted Android runner validation.

## Evidence

Native Android evidence is available under:

```text
examples/artifacts/real-runs/native-android-login/
```

The run covers:

- Genymotion Galaxy S24.
- `com.example.mobiloopnative`.
- UiAutomator2.
- Login flow validation.
- `Ana Sayfa` and `Hos geldiniz` expected text checks.
- Screenshot, page source, logcat, iterations, and metrics artifacts.

## Container Images

Expected tags:

```text
ghcr.io/enessubass/mobiloop-mcp:0.1.0-alpha.5
ghcr.io/enessubass/mobiloop-mcp:v0.1.0-alpha.5
ghcr.io/enessubass/mobiloop-mcp:latest
```

## Known Follow-Ups

- Validate the Docker Node 26 runtime bump before merging Dependabot PR #1.
- Validate GitHub Actions major-version compatibility on the self-hosted Android runner before merging Dependabot PR #2.
- Add React Native and iOS Swift fixtures.
- Add iOS validation orchestration parity.
